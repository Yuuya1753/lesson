<h2>フォロワーリスト</h2>

<% if @followers.empty? %>
	<%= "フォロワーはいません。" %>
<% else %>
	<% @followers.each do |follower| %>
		<% if @user.email != follower.email %>
			<%= link_to follower.email, "/#{follower.account}" %>
			<br>
		<% end %>
	<% end %>
<% end %>